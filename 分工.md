# 项目分工

## 前端层：

负责通过Wails衔接后端服务逻辑，并通过Vue完成前端构造。

## 逻辑层：王子心

负责构造核心循环、组建事件逻辑、维护元数据数据库和加密，并构造软件的骨架。

## 网络层：刘家豪

负责处理所有网络功能，从mDNS自动发现到信封机制，再到文件的网络传输。

## 文件层：张子佳

负责处理所有与文件系统交互的功能，包括目录监视、元数据目录生成、无读入内存文件传递等等。

