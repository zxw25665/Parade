# “游行”：去中心化分布式局域网加密协作软件设计书

## 简介

本软件旨在消灭协作的“最后一公里”：明明在同一个局域网下，内部网络畅通无阻且高速，但我们却没有一个方便好用的本地化协作方案。

* 用U盘？每次只能一台设备读取数据，需要物理意义上的走来走去，且若文件有了更新整个过程还要再来一次。
* 用社交软件？且不说我们是否信任大公司掌握我们的数据，为什么需要把文件先送到他们的服务器、再从服务器发回来？这显得非常多余，更不用说“14天删除”和“文件小于1GB”的限制了。
* 用SMB等共享协议？配置起来非常不便，需要分发密码，且性能并不稳定。况且，这只能由一个人作为中心，如果一个团队之间需要互相传递数据，显然不能每个人各自设立一个SMB服务、给其余的所有人各自配置一个密码。

“游行”旨在解决这些问题。软件的目标是提供一个易于安装部署和操作的一站式协作平台。具体功能特点如下：

* 完全去中心化 - 没有必须的任何硬件投资，不需要为了一场小组作业租用云服务器或购入软路由。
* 端到端加密 & 本地磁盘加密 - 阻止有心人窃取你的信息，无论是在本地还是在云端。
* 易于使用的通信协作 - 创建你的队伍，与成员私聊或组建包含任意成员的群聊，就像微信一样简单。
* 高性能文件共享 - 每个人都是参与者。选定你的共享目录，控制成员权限，点击共享即可，从此忘掉SMB的账号密码和繁琐操作。成员可以从本地拉取文件或上传到本地，无论文件大小多少，如丝般顺滑。

## 整体系统架构

系统整体分为三个逻辑切面：

* 发现面：负责在局域网中寻找开启软件的节点 IP。
* 控制面（端口4327）建立gRPC长连接，负责所有轻量级信令（握手、心跳、聊天、元数据同步），由混合逻辑时钟 (HLC) 保证强一致性。
* 数据面（端口4328）：独立通道，专为大文件 I/O 优化的流式传输，防止队头阻塞。

### 前端交互

“游行”的前端致力于提供媲美原生桌面应用的流畅体验，完全屏蔽底层分布式网络的复杂性，同时保证跨平台一致性。

* 状态驱动的UI：前端采用Vue3 + Pinia进行全局状态管理。底层的节点发现、掉线、消息接收等网络事件，均通过 Wails 的 IPC（进程间通信）机制实时推送到前端，UI 能够做到毫秒级无感刷新。
* 分栏式工作区：界面设计以效率为核心，分为三大功能区：左侧的动态成员与队伍列表（实时反映局域网内的拓扑状态），中间的消息流面板（按照混合逻辑时钟严格排序的群聊/私聊），以及右侧的分布式文件树面板。
 * 网络拓扑与会话（左侧）：动态显示局域网内当前在线成员，通过绿/灰圆点标识心跳状态。
 * 消息流（中部）：严格按照HLC排序的消息流，支持系统级通知（如“XXX已加入队伍”）和MarkDown渲染。
 * 虚拟文件树（右侧）：采用懒加载的树状组件，每个成员都有一个根，可进行任意层级展开。
* 零配置连接感知：前端提供了一个直观的网络雷达界面。当用户输入队伍口令后，UI 会展示发现进度的三级降级状态（mDNS搜索 -> 子网遍历 -> 提示输入直连口令），所有底层探测对用户保持透明且友好。

### 文件共享

在文件共享层面，“游行”抛弃了传统的中心化文件服务器或全局 VFS（虚拟文件系统），转而采用“分布式多中心与按需拉取”模型：

* 去中心化文件树：每个成员都可以将本地的任意目录暴露给当前队伍。软件不会将真实文件推送到网络中，而是仅在内存中生成一层极轻量的“虚拟元数据目录树”（包含文件名、大小、类型），并在有权限的对方申请查看时实现“秒加载”。
* 按需浏览：当其他成员在 UI中点击展开某个共享文件夹时，才会向源节点发起RPC请求拉取下一层级的元数据。这极大地节省了局域网带宽，且能轻松支持十万级以上文件的海量目录共享。
* 绝对所有权：成员节点之间完全平等。共享文件的节点即为该目录的唯一服务器，文件修改、删除、共享权限的撤销均在本地瞬间生效，无需全网协商。
* 可控文件接收：成员可以自行设定标准，阻断所有大于这个标准大小的文件被直接推入本地并询问是否接收。同时，成员可以选择将文件上传到本地特定目录或对方特定目录，非常灵活。

### 网络通信

“游行”的网络通信模块分为三个部分：发现部分、标准信息传输部分、文件传输部分。

#### 发现部分：

“游行”采用一个三层机制进行局域网下的互相发现。

* mDNS/UDP广播：这是标准机制，也是默认机制。
* IPv4 UDP遍历：这是在AP隔离的IPv4网络环境下采用的回退机制。“游行”会检测网络的子网掩码，若为/24或更高则会进行速度受控的遍历式“敲门”发现。
* 手动输入口令发现：这是最后的可用方案。房间内成员可以导出一个由房间口令+客户端IP地址生成的口令，新加入成员可以通过输入这个口令加入房间。

#### 标准信息传输部分：

“游行”将软件正常运作所传递的信息分为两部分，此处讲解标准信息传递部分。这里涵盖除了文件传递外的其他一切网络通信，包括软件心跳、聊天传输、后台同步等等。

信息采用混合逻辑时钟（HLC）进行顺序确认。由于完全去中心化网络缺乏统一的时间裁判，HLC完美结合了物理时钟的直观性和逻辑时钟（Lamport）的因果一致性。全平台通过UTC标准时进行物理时间同步。

HLC：二维，（UTC_Mili，Node_ID）。

标准信息传输部分在端口4327上建立TCP连接进行通信。得益于底层的多路复用特性，针对单个节点的所有标准信息（心跳、聊天报文、状态同步）均通过这一条长连接收发，避免频繁的TCP握手消耗，极大地降低了网络延迟和系统开销。技术栈采用gRPC over HTTP/2，采用双向流。

除了发现层外的所有文件传输都在统一的包裹下完成，以“信封”的格式传递，包括类型和内容。

#### 文件传输部分：

为防止大文件传输阻塞正常的聊天和心跳信令，“游行”的文件传输部分在逻辑上与标准信息传输分离，工作于独立的4328端口，并针对大吞吐量 I/O 进行了专门的优化：

* 流式切片与零拷贝：文件在发送端被实时切分为固定大小的内存块（Chunk，2MB/块），利用Go语言底层的 io.CopyBuffer 直接对接网络流发送，避免将几 GB 的大文件一次性读入内存导致软件崩溃。
* 断点续传与校验：接收端在本地记录已下载的字节偏移量。如果局域网发生波动导致连接中断，恢复连接后接收端只需请求未下载的剩余块。同时，每块数据附带校验码，确保组装后的文件 100% 完整无损。
* 点对点直连并行：文件下载是严格的点对点（P2P）行为。如果成员 C 向成员 A 请求文件，数据流将在 A 与 C 之间直接建立传输，不经过任何第三方的路由转发，轻松跑满千兆/万兆局域网带宽。

基于gRPC Server Streaming，发送端按2MB/块读取文件，通过io.CopyBuffer直接写入网络流，避免高内存占用；请求下载时携带偏移量，若传输中断，接收端可查阅SQLite中的已接收大小，重新发起请求即可断点中继。

### 信息存储

既然没有云端服务器，所有的数据沉淀必须在本地完成。“游行”内置了无依赖的纯 Go SQLite 作为本地存储引擎。

* 双表结构：数据库核心围绕两张表展开：messages（存储按 HLC 排序的聊天记录）和 file_logs（存储文件的发送/接收历史与断点状态）。
* 离线可用：由于数据存储在本地，用户即使断开网络，依然可以打开软件查阅以往的队伍聊天记录和传输日志。
* 同步对账机制：当节点重新连入局域网时，会读取本地 SQLite 中记录的最后一条 HLC 时钟，并向队伍中其他成员发起“对账”。若发现本地数据落后，则自动拉取断网期间缺失的消息入库。

基于纯Go实现的modernc.org/sqlite，无需CGO。

### 数据加密

“游行”的数据加密模块分三个部分：落盘加密、队伍加密、成员加密。

首先，游行一共有以下三种密钥：成员密钥（非对称），队伍密钥（对称），用户主密钥（对称）。

* 成员密钥依据成员在登入软件时输入的账号和密码转换得出（注意，这并非在线服务，只用于演算密钥！），分公钥和私钥。
* 队伍密钥由队伍创立者设置的口令转换得出，用于在队伍范围内进行数据的端到端加密，以保护在局域网内部传递的信息。
* 用户主密钥为依据私钥派生出的对称密钥。

#### 落盘加密：

除了传递的文件外，所有应用元数据和聊天信息数据在存入磁盘前都会经过用户主密钥的加密。成员私钥不会存入磁盘，只存在于内存中，因此每次软件重启后都需要从头演算出私钥并基于此演算出成员主密钥用以解密。对于任何窃取者或旁听者而言，如果不掌握账号密码对，数据是不可能被解密的。对真正的用户也是一样，所以务必不要忘记自己的账号密码。一旦忘记，数据没有任何恢复办法。

#### 队伍加密：

针对网络模块中的标准信息传输部分和文件传输部分，只要是从某个节点发出的数据，必然会经过队伍密钥的对称加密。

#### 成员加密：

在队伍群聊之外，“游行”支持成员之间的私聊。这类消息在经过队伍对称密钥加密的基础之上，还会根据目标成员的公钥进行二次包裹加密（双重加密）。这确保了即便其他成员拥有相同的队伍口令，也绝对无法解密和旁听私聊的私密对话。

## 附录：第三方开源依赖清单

| 逻辑模块 | 核心功能 | 推荐开源库 (Go/JS) | 引入原因与应用场景 |
| :--- | :--- | :--- | :--- |
| **桌面端外壳** | 跨平台桌面集成 | `github.com/wailsapp/wails/v2` | 提供极简的 Go 与前端 JS/TS 的双向 IPC 通信，取代笨重的 Electron。 |
| **前端 UI 框架** | 视图与状态管理 | `Vue 3` + `Pinia` | Vue3 响应式渲染，Pinia 负责全局存储（在线列表、聊天记录、下载进度）。 |
| **发现引擎** | mDNS 局域网广播 | `github.com/hashicorp/mdns` | HashiCorp 官方库，零配置实现 `_parade._tcp` 局域网组播发现。 |
| **发现引擎** | P2P 状态与存活检测 | `github.com/hashicorp/memberlist` | 基于 SWIM Gossip 协议，完美解决成员掉线检测和 IP 列表同步。 |
| **信令与数据面** | P2P RPC 通信底座 | `google.golang.org/grpc` | HTTP/2 多路复用，双向流支持，分设 4327 和 4328 两个独立端口通信。 |
| **信令与数据面** | 数据序列化 | `google.golang.org/protobuf` | 结构化数据定义，体积小、解析极快，作为全队接口契约（Contract）。 |
| **密码学模块** | 主密钥派生 | `golang.org/x/crypto/argon2` | 业界标杆的防内存/GPU暴力破解算法，用于将“密码”转换为32字节对称密钥。 |
| **密码学模块** | 端到端加密机制 | 标准库 `crypto/cipher` | 使用 `AES-GCM (256-bit)` 作为 gRPC 的载荷加密，有硬件加速，性能极高。 |
| **密码学模块** | 身份与私聊协商 | `golang.org/x/crypto/curve25519` | 极速椭圆曲线非对称加密，用于节点身份标识与私聊的会话密钥协商。 |
| **本地数据库** | 离线数据沉淀 | `modernc.org/sqlite` | **极其重要！** 纯 Go 实现的 SQLite，免除 CGO 编译地狱，完美适配 Wails。 |
| **文件传输** | 大文件 I/O 优化 | 标准库 `io` (`io.CopyBuffer`) | 使用固定大小（如 2MB）的缓冲池切片读取磁盘，实现零内存泄露流传输。 |
| **文件传输** | 分块防篡改校验 | `github.com/zeebo/blake3` | 目前已知最快的哈希算法（比 SHA-256 快数十倍），用于大文件分块完整性校验。 |
