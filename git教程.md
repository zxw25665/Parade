# 如何利用git进行编辑

## 0. 首先，需要保证网络通畅。

如果你的代理软件有虚拟网卡模式（TUN），请将其打开，这会一劳永逸地解决所有问题。

如果你不希望开启TUN，那么可以选择通过在终端中输入以下命令激活当前终端中的代理：

Windows：
```powershell
$env:HTTP_PROXY="http://127.0.0.1:端口号"
$env:HTTPS_PROXY="http://127.0.0.1:端口号"
```

macOS & Linux：
```bash
export http_proxy="http://127.0.0.1:端口号"
export https_proxy="http://127.0.0.1:端口号"
```

其中，端口号需要从自己的代理软件获取。

## 0.5 不必多说，你需要下载git才能使用git。

在Windows上，建议直接从网站下载安装包；

在macOS或Linux上，用homebrew、apt或其他包管理器直接安装即可。

在完成安装后，你需要配置git以让其知道你是谁。这用于在提交记录中标识贡献者。
```bash
git config --global user.name "你希望被人称呼的名字"
git config --global user.email "你希望使用的邮箱"
```

最后，在你真正开始参与开发之前，将远程仓库克隆到本地。
```bash
git clone <仓库的url地址>
```

## 1. 标准开发流程：分支管理

严禁直接在main分支上修改代码！无论改动大小，务必遵循以下工作流：

### 第一步：同步远程最新代码

在开始新工作前，先回到主分支并拉取最新更改：
```bash
# 注意，若你当前就在main分支且有未提交的代码，git pull可能失败。务必确保在执行前工作区是干净的。
git checkout main
git pull origin main
```

### 第二步：创建功能分支

根据你要进行的工作（如完成模块或程序，抑或修复bug），起一个有意义的分支名：
```bash
git checkout -b 有意义的名称（不能带空格或特殊字符）
```

### 第三步：本地开发与提交

在使用git开发的过程中，不建议每一小步代码都发起PR，但同样不建议一直不提交。最好在本地以“小步快跑”模式频繁提交：
```bash
# 查看改动状态
git status

# 将改动添加到暂存区
git add .

# 提交并附带清晰的说明
git commit -m "此处为清晰说明"
```

### 第四步：推送分支到远程

这是真正让你的代码从本地回到github服务器的方式。注意，此时你发回的是你的分支，而非直接归入主干。
```bash
git push origin 你的分支名
```

## 2. 合并代码：发起Pull Request（PR）

代码推送到远程后，严禁直接合并入主线，这会带来不可预知的风险。请在网页上操作：

 1. 点击Compare & pull request按钮。
 2. 指派一名队友作为审查者。
 3. 经过代码审查且测试通过后，由负责人合并入主分支。
 4. 在合并完成后，记得回到本地main分支拉取最新代码，并删除本地的功能分支，保持本地开发环境简洁。

## 3. 处理代码冲突

在利用github的协作场景下，代码冲突是常事。最根本的避免方式就是频繁发起PR并保证主线的先进性，但在冲突已经发生时我们可以遵循以下方法：

 1. 打开提示冲突的文件，寻找`<<<<<<<HEAD`和`>>>>>>>`标记。
 2. 手动决定保留哪部分代码，然后删除标记。
 3. 保存文件后执行：
 ```bash
 git add <冲突文件名>
 git commit -m "修复：解决xx文件合并冲突"
 ```

若使用VS Code，冲突有直接的UI解决方案，建议使用。

## 4. 写错代码了，希望回退

git的好处就在于此。

在还没有commit时：撤销某个文件的修改，用命令`git restore <文件名>`

在已经commit但尚未push时：撤销上一次提交，保留代码更改，用命令`git reset HEAD~1`

## 5. 实践

现在，同学们需要完整走一遍开发流程，在 分工.md 里填入自己想要的分工位置。先到先得，发起PR后视为选定成功，不可重复选择。
